
local PLUGIN = PLUGIN

ix.radio.RegisterChannel("combine", {
	color = Color(200, 0, 0, 255)
})

ix.radio.RegisterChannel("cab", {
	color = Color(200, 0, 0, 255)
})

ix.radio.RegisterChannel("cp_main", {
	color = Color(0, 144, 188, 255)
})

ix.radio.RegisterChannel("overwatch", {
	color = Color(0, 144, 188, 255)
})

ix.radio.RegisterChannel("ptf", {
	color = Color(200, 200, 50, 255)
})

ix.radio.RegisterChannel("peo", {
	color = Color(95, 170, 200, 255)
})

ix.radio.RegisterChannel("uac", {})
